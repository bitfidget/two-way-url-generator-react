const GenerateCheckedItemsString = (items) => {
    // iterate through the items list and create a url string to represent the items that are checked
    return null
}

export default GenerateCheckedItemsString